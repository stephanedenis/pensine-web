# Architecture @panini/plugin-interface

## Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────┐
│                    Panini Ecosystem                             │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Pensine    │  │  OntoWave    │  │  PaniniFS    │          │
│  │   (notes)    │  │ (navigator)  │  │ (filesystem) │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                  │
│         └─────────────────┼─────────────────┘                  │
│                           │                                    │
│         ╔═════════════════v════════════════════╗               │
│         ║  @panini/plugin-interface v0.1.0    ║               │
│         ║  (Common TypeScript interfaces)     ║               │
│         ╚═════════════════════════════════════╝               │
│                           │                                    │
│         ┌─────────────────┼─────────────────┐                  │
│         │                 │                 │                  │
│    ┌────v─────┐     ┌────v─────┐     ┌────v─────┐             │
│    │  Plugin  │     │  Plugin  │     │  Plugin  │             │
│    │ PlantUML │     │ Mermaid  │     │Calendar  │             │
│    └──────────┘     └──────────┘     └──────────┘             │
│                                                                 │
│  🎯 Write once, run in all 3 apps!                             │
└─────────────────────────────────────────────────────────────────┘
```

## Architecture détaillée

```
┌────────────────────────────────────────────────────────────────────┐
│                         PaniniPlugin                               │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  manifest: PaniniPluginManifest                          │      │
│  │    - id, name, version, tags, dependencies               │      │
│  │                                                           │      │
│  │  activate(context: PaniniPluginContext): Promise<void>   │      │
│  │  deactivate(): Promise<void>                             │      │
│  │  onConfigChange?(config: any): Promise<void>             │      │
│  │  healthCheck?(): Promise<boolean>                        │      │
│  └──────────────────────────────────────────────────────────┘      │
└────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ context injected at activation
                                   │
                                   v
┌────────────────────────────────────────────────────────────────────┐
│                    PaniniPluginContext                             │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  app: 'pensine' | 'ontowave' | 'panini-fs'              │      │
│  │  version: string                                         │      │
│  │                                                           │      │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │      │
│  │  │   EventBus   │  │ConfigManager │  │StorageAdapter│   │      │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │      │
│  │  ┌──────────────┐  ┌──────────────┐                     │      │
│  │  │FeatureFlags  │  │    Logger    │                     │      │
│  │  └──────────────┘  └──────────────┘                     │      │
│  │                                                           │      │
│  │  user?: UserInfo                                         │      │
│  └──────────────────────────────────────────────────────────┘      │
└────────────────────────────────────────────────────────────────────┘
              │                   │                    │
              │                   │                    │
              v                   v                    v
     ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
     │    EventBus    │  │ConfigManager   │  │StorageAdapter  │
     │                │  │                │  │                │
     │ on(event, fn)  │  │getCoreConfig() │  │readFile(path)  │
     │ emit(event)    │  │getPluginConfig │  │writeFile(...)  │
     │ clearNamespace │  │registerSchema  │  │listFiles(path) │
     │                │  │validate(cfg)   │  │semanticSearch? │
     └────────────────┘  └────────────────┘  └────────────────┘
```

## Exemple concret: Word Counter Plugin

```
┌─────────────────────────────────────────────────────────────┐
│              WordCounterPlugin                              │
│  implements PaniniPlugin                                    │
│                                                             │
│  manifest = {                                               │
│    id: 'word-counter',                                      │
│    name: 'Word Counter',                                    │
│    version: '1.0.0'                                         │
│  }                                                          │
│                                                             │
│  activate(context) {                                        │
│    ┌────────────────────────────────────────┐              │
│    │ 1. Register config schema              │              │
│    │    context.config.registerSchema(...)  │              │
│    └────────────────────────────────────────┘              │
│                     │                                       │
│                     v                                       │
│    ┌────────────────────────────────────────┐              │
│    │ 2. Get plugin config                   │              │
│    │    const cfg = context.config          │              │
│    │      .getPluginConfig('word-counter')  │              │
│    └────────────────────────────────────────┘              │
│                     │                                       │
│                     v                                       │
│    ┌────────────────────────────────────────┐              │
│    │ 3. Subscribe to events                 │              │
│    │    context.events.on('file:opened',    │              │
│    │      handler, 'word-counter')          │              │
│    │                                        │              │
│    │    context.events.on('markdown:render',│              │
│    │      handler, 'word-counter')          │              │
│    └────────────────────────────────────────┘              │
│  }                                                          │
│                                                             │
│  deactivate() {                                             │
│    // Auto cleanup via clearNamespace('word-counter')      │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
```

## Flow d'événements

```
┌──────────────┐
│     User     │
└──────┬───────┘
       │ 1. Opens file
       v
┌──────────────────────────────┐
│   Pensine/OntoWave App       │
│                              │
│  context.events.emit(        │
│    'file:opened',            │
│    { path: 'my-note.md' }    │
│  )                           │
└──────┬───────────────────────┘
       │ 2. Event propagated
       v
┌──────────────────────────────┐
│       EventBus               │
│                              │
│  handlers['file:opened']     │
│    .forEach(h => h(data))    │
└──────┬───────────────────────┘
       │ 3. Handler called
       v
┌──────────────────────────────┐
│    WordCounterPlugin         │
│                              │
│  async onFileOpened(path) {  │
│    const content = await     │
│      context.storage         │
│        .readFile(path);      │────┐
│                              │    │ 4. Read file
│    updateCounts(content);    │    │
│                              │    v
│    context.events.emit(      │  ┌────────────────────┐
│      'word-counter:updated', │  │ StorageAdapter     │
│      { wordCount, charCount }│  │                    │
│    );                        │  │ GitHub/Local/...   │
└──────┬───────────────────────┘  └────────────────────┘
       │ 5. Stats updated
       v
┌──────────────────────────────┐
│      UI Component            │
│                              │
│  Shows: "325 words, 1842 ch" │
└──────────────────────────────┘
```

## Lifecycle d'un plugin

```
┌─────────────┐
│  UNLOADED   │ Initial state
└──────┬──────┘
       │ 1. Plugin created
       │    new MyPlugin()
       v
┌─────────────┐
│   LOADED    │ Ready to activate
└──────┬──────┘
       │ 2. activate(context) called
       │    - Register schema
       │    - Subscribe to events
       │    - Initialize state
       v
┌─────────────┐
│   ACTIVE    │◄──────┐ Running
└──────┬──────┘       │
       │              │ 3. Optional:
       │              │    onConfigChange()
       │              │    healthCheck()
       │              └────┘
       │
       │ 4. deactivate() called
       │    - clearNamespace()
       │    - Cleanup resources
       v
┌─────────────┐
│DEACTIVATING │ Cleanup in progress
└──────┬──────┘
       │
       v
┌─────────────┐
│  UNLOADED   │ Stopped
└─────────────┘

       ERROR   │ If activation fails
       └───────┘
```

## Namespace cleanup pattern

```
BEFORE (problématique):
┌────────────────────────────────────────┐
│           EventBus                     │
│                                        │
│  handlers = {                          │
│    'file:opened': [                    │
│      handler1,  // Plugin A            │
│      handler2,  // Plugin B            │
│      handler3   // Plugin A again      │
│    ]                                   │
│  }                                     │
│                                        │
│  ❌ deactivate() of Plugin A:         │
│     Must manually remove handler1      │
│     and handler3, but how?             │
└────────────────────────────────────────┘

AFTER (avec namespace):
┌────────────────────────────────────────┐
│           EventBus                     │
│                                        │
│  handlers = {                          │
│    'plugin-a:file:opened': [           │
│      handler1,                         │
│      handler3                          │
│    ],                                  │
│    'plugin-b:file:opened': [           │
│      handler2                          │
│    ]                                   │
│  }                                     │
│                                        │
│  ✅ deactivate() of Plugin A:         │
│     clearNamespace('plugin-a')         │
│     → All handlers removed!            │
└────────────────────────────────────────┘
```

## Config hierarchy

```
┌────────────────────────────────────────────────────┐
│               ConfigManager                        │
│                                                    │
│  ┌──────────────────────────────────────────┐     │
│  │ Core Config (app-level)                  │     │
│  │                                           │     │
│  │  {                                        │     │
│  │    theme: 'dark',                         │     │
│  │    language: 'en',                        │     │
│  │    autoSave: true                         │     │
│  │  }                                        │     │
│  └──────────────────────────────────────────┘     │
│                                                    │
│  ┌──────────────────────────────────────────┐     │
│  │ Plugin Configs (namespaced)              │     │
│  │                                           │     │
│  │  'word-counter': {                        │     │
│  │    enabled: true,                         │     │
│  │    showCharCount: true,                   │     │
│  │    updateInterval: 500                    │     │
│  │  },                                       │     │
│  │                                           │     │
│  │  'plantuml': {                            │     │
│  │    serverUrl: 'https://...',              │     │
│  │    theme: 'default'                       │     │
│  │  }                                        │     │
│  └──────────────────────────────────────────┘     │
│                                                    │
│  ┌──────────────────────────────────────────┐     │
│  │ Schemas (JSON Schema validation)         │     │
│  │                                           │     │
│  │  'word-counter': {                        │     │
│  │    type: 'object',                        │     │
│  │    properties: {                          │     │
│  │      enabled: { type: 'boolean' },        │     │
│  │      updateInterval: {                    │     │
│  │        type: 'number',                    │     │
│  │        minimum: 100                       │     │
│  │      }                                    │     │
│  │    }                                      │     │
│  │  }                                        │     │
│  └──────────────────────────────────────────┘     │
└────────────────────────────────────────────────────┘
```

## Storage abstraction

```
┌────────────────────────────────────────────────────┐
│            StorageAdapter Interface                │
│                                                    │
│  readFile(path): Promise<string>                   │
│  writeFile(path, content, msg?): Promise<void>     │
│  deleteFile(path, msg?): Promise<void>             │
│  listFiles(path): Promise<string[]>                │
│  fileExists(path): Promise<boolean>                │
│  getFileMetadata?(path): Promise<FileMetadata>     │
│  semanticSearch?(query): Promise<SearchResult[]>   │
└────────┬────────────────────────────────┬──────────┘
         │                                │
         v                                v
┌─────────────────┐          ┌─────────────────────┐
│ GitHubAdapter   │          │ LocalGitAdapter     │
│                 │          │                     │
│ Uses GitHub API │          │ Uses git commands   │
│ REST calls      │          │ Local filesystem    │
└─────────────────┘          └─────────────────────┘
         │                                │
         └────────────┬───────────────────┘
                      │
                      v
         ┌─────────────────────────┐
         │   PaniFinderAdapter     │
         │   (futur)               │
         │                         │
         │ Semantic search         │
         │ Metadata enrichment     │
         └─────────────────────────┘
```

## Cross-app compatibility

```
┌───────────────────────────────────────────────────┐
│         Same plugin, 3 different apps             │
│                                                   │
│  ┌─────────────────────────────────────────┐     │
│  │  @panini/plugin-word-counter            │     │
│  │  (compiled once)                        │     │
│  └──────┬──────────────────────────────────┘     │
│         │                                         │
│    ┌────┴────────────┬────────────────┐          │
│    │                 │                │          │
│    v                 v                v          │
│  ┌──────┐        ┌────────┐      ┌────────┐     │
│  │Pensine│       │OntoWave│      │PaniniFS│     │
│  └───┬───┘       └───┬────┘      └───┬────┘     │
│      │               │               │          │
│      v               v               v          │
│  Context:       Context:        Context:        │
│  app:'pensine'  app:'ontowave'  app:'panini-fs' │
│  storage:GitHub storage:Local   storage:PaniniFS│
│  offline:true   hotReload:true  semantic:true   │
│                                                   │
│  Same code, different context!                   │
└───────────────────────────────────────────────────┘
```

---

**Légende**:

- `┌─┐ └─┘` : Composants
- `│ ─ ` : Relations/flow
- `▼ ►` : Direction
- `╔═╗ ╚═╝` : Interfaces clés
- `?` : Méthodes optionnelles
