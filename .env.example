# GitHub OAuth App Configuration
# Créer une OAuth App sur https://github.com/settings/developers

# Client ID de votre GitHub OAuth App
GITHUB_OAUTH_CLIENT_ID=Iv1.YOUR_CLIENT_ID_HERE

# URL de callback OAuth (doit correspondre à la config GitHub)
OAUTH_CALLBACK_URL=https://votre-domaine.com/oauth-callback.html

# URL du backend Cloudflare Worker
OAUTH_BACKEND_URL=https://pensine-oauth.YOUR_SUBDOMAIN.workers.dev

# ⚠️ SECRETS (NE PAS COMMITER) ⚠️
# Ces valeurs doivent être configurées via `wrangler secret put`

# Client Secret GitHub (obtenu lors de la création de l'OAuth App)
# wrangler secret put GITHUB_CLIENT_SECRET
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Clé pour signer les JWT (générer avec `openssl rand -base64 32`)
# wrangler secret put JWT_SECRET
JWT_SECRET=your_random_jwt_secret_here

# ============================================================
# TEST CREDENTIALS (Playwright E2E tests)
# ============================================================
#
# GitHub Personal Access Token pour les tests E2E
# Créer sur: https://github.com/settings/tokens
# Permissions: repo (full access)
GITHUB_TEST_TOKEN=ghp_your_test_token_here

# Username GitHub pour les tests
GITHUB_TEST_OWNER=your_username

# Repository pour les tests
GITHUB_TEST_REPO=pensine-web

# ============================================================
# INSTRUCTIONS
# ============================================================
#
# 1. Copier ce fichier en `.env` (ne PAS commiter .env)
# 2. Remplir les valeurs GITHUB_OAUTH_CLIENT_ID, OAUTH_CALLBACK_URL, OAUTH_BACKEND_URL
# 3. Les secrets doivent être configurés avec Wrangler CLI, PAS dans .env
# 4. Pour les tests: export GITHUB_TEST_TOKEN="ghp_..." avant de lancer les tests

#
# Pour configurer les secrets dans Cloudflare Workers :
#   cd workers/
#   wrangler secret put GITHUB_CLIENT_SECRET
#   wrangler secret put JWT_SECRET
#
# Pour tester localement :
#   wrangler dev
#
# Pour déployer en production :
#   wrangler deploy
#
# Documentation complète : docs/OAUTH_SETUP.md
